server:
  port: 8087

spring:
  application:
    name: notification-service

logging:
  level:
    root: INFO
    com.banking: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"
